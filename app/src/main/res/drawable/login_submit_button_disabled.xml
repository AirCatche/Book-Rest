<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:angle="180"
        android:startColor="@color/accent_disabled"
        android:centerColor="@color/accent_disabled"
        android:endColor="@color/accent_disabled"
        android:type="linear" />
    <corners android:radius="50dp" />
</shape>
